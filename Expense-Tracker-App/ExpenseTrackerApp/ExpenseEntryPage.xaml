<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="Expense Entry"
             x:Class="ExpenseTrackerApp.ExpenseEntryPage"
             Padding="10"
             BackgroundColor="SkyBlue">
    <ContentPage.Content>
<<<<<<< HEAD
        <!-- The user should be shown a modal that allows them to input details for their expenses:
        the name, amount, date the purchase was made on, and an icon to categorize the item.
        The modal should allow the user to either cancel or save the entry that they make.-->

        <StackLayout Orientation="Vertical" VerticalOptions="FillAndExpand" Spacing="10">

            <StackLayout Spacing="0">
                <Label Text="Description" TextColor="Black" FontSize="24"/>
                <Entry x:Name="name" Placeholder="Expense Name" FontSize="24" ClearButtonVisibility="WhileEditing"
                   Keyboard="Text"/>
            </StackLayout>
=======
        
        <StackLayout>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Label Text="Name" />
                <Entry x:Name="name" Placeholder="Expense Name" ClearButtonVisibility="WhileEditing"
                   Keyboard="Text" />
>>>>>>> master

            <StackLayout Spacing="0">
                <Label Text="Amount" TextColor="Black" FontSize="24"/>
                <Entry x:Name="amount" Placeholder="$0.00" FontSize="24" ClearButtonVisibility="WhileEditing" Keyboard="Numeric"/>
            </StackLayout>

            <StackLayout Spacing="0">
                <Label Text="Date" TextColor="Black" FontSize="24"/>
                <DatePicker x:Name="expensedate" Format="d" FontSize="24" VerticalOptions="CenterAndExpand"/>
            </StackLayout>

<<<<<<< HEAD
            <StackLayout Spacing="0">
                <Label Text="Category" TextColor="Black" FontSize="24"/>
                <Picker x:Name="category"
                        Title="Select Category"                        
                        FontSize="24"
                        ItemsSource="{Binding CategoriesList}"
                        ItemDisplayBinding="{Binding Name}" />
                <!-- Neeeds be changed to IconFilepath-->
            </StackLayout>                

            <StackLayout Orientation="Horizontal" Spacing="40" HorizontalOptions="CenterAndExpand">
                <Button Text="Save" TextColor="Black" FontSize="24" BackgroundColor="Gray" Clicked="OnSaveButtonClicked" />
                <Button Text="Cancel" TextColor="Black" FontSize="24" BackgroundColor="Gray" Clicked="OnCancelButtonClicked" />
            </StackLayout>            
            
=======
                <Label Grid.Row="3" Text="Category" />

                <ListView Grid.Row="3" x:Name="selectCategory">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ImageCell Text="{Binding Name}"
                                       ImageSource="{Binding Image} "/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Grid Grid.Row="4" x:Name="buttons">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Text="Save" Clicked="OnSaveButtonClicked" />
                    <Button Text="Cancel" Grid.Column="1" Clicked="OnCancelButtonClicked" />
                </Grid>
            </Grid>
>>>>>>> master
        </StackLayout>

    </ContentPage.Content>
</ContentPage>